name: TestAndBuildGo17

on:
  pull_request:
    branches: [ master ]

env:
  CARGO_TERM_COLOR: always

jobs:
  verify-go17:
    runs-on: ubuntu-latest

    outputs:
      build: ${{ steps.changed.outputs.build }}

    steps:
      - uses: actions/checkout@v2

      - name: Fetch
        run: git fetch

      - name: Set Enviromnent Variable
        shell: bash
        run: echo "CHANGED=$(git diff --quiet HEAD origin/master -- go17 || echo true)" >> $GITHUB_ENV

      - name: Check For Changes
        id: changed
        if: ${{ env.CHANGED == 'true' }}
        run: echo "::set-output name=build::true"

  verify-go:
    runs-on: ubuntu-latest

    outputs:
      build: ${{ steps.changed.outputs.build }}

    steps:
      - uses: actions/checkout@v2

      - name: Fetch
        run: git fetch

      - name: Set Enviromnent Variable
        shell: bash
        run: echo "CHANGED=$(git diff --quiet HEAD origin/master -- go || echo true)" >> $GITHUB_ENV

      - name: Check For Changes
        id: changed
        if: ${{ env.CHANGED == 'true' }}
        run: echo "::set-output name=build::true"

  build-go17:
    needs: verify-go17
    if: contains(needs.verify-go17.outputs.build, 'true')
    runs-on: ubuntu-latest

    outputs:
      complete: ${{ steps.build.outputs.complete }}

    steps:
      - name: Thingy
        run: echo ${{ needs.verify-go17.outputs.build }}

      - name: Set Enviromnent Variable
        shell: bash
        run: echo "COMPLETE=true" >> $GITHUB_ENV

      - name: Echo COMPLETE Environment Variable
        run: echo ${{ env.COMPLETE }}

      - name: Check For Changes
        id: build
        if: ${{ env.COMPLETE == 'true' }}
        run: echo "::set-output name=complete::true"

#      - uses: actions/checkout@v2
#
#      - uses: themeliolabs/actions-biome@v1
#        with:
#          BIOME_PUBLIC_KEY: ${{ secrets.BIOME_PUBLIC_KEY }}
#          BIOME_SIGNING_KEY: ${{ secrets.BIOME_SIGNING_KEY }}
#
#      - name: Cache Biome
#        uses: actions/cache@v2
#        env:
#          CACHE_NAME: cache-biome
#        with:
#          path: ~/.hab/cache
#          key: ${{ runner.os }}-build-${{ env.CACHE_NAME }}-${{ hashFiles('go17/plan.sh') }}
#          restore-keys: |
#            ${{ runner.os }}-build-${{ env.CACHE_NAME }}-
#            ${{ runner.os }}-build-
#            ${{ runner.os }}-

  build-go-if-go17-changed:
    needs:
      - verify-go17
      - verify-go
      - build-go17
    if: contains(needs.verify-go.outputs.build, 'true') && contains(needs.verify-go.outputs.build, 'true') && contains(needs.build-go17.outputs.complete, 'false')
    runs-on: ubuntu-latest

    steps:
      - name: Show Go17 Verify Output
        run: echo ${{ needs.verify-go17.outputs.build }}

      - name: Show Go17 Build Output
        run: echo ${{ needs.build-go17.outputs.complete }}



# I need to duplicate the go build step:
# build-go-go17-changed and build-go





#  build:
#    if: ${{ env.CHANGED == 'true' }}
#
#    runs-on: ubuntu-latest
#
#    steps:
#      - uses: actions/checkout@v2
#
#      - uses: themeliolabs/actions-biome@v1
#        with:
#          BIOME_PUBLIC_KEY: ${{ secrets.BIOME_PUBLIC_KEY }}
#          BIOME_SIGNING_KEY: ${{ secrets.BIOME_SIGNING_KEY }}
#
#      - name: Cache Biome
#        uses: actions/cache@v2
#        env:
#          CACHE_NAME: cache-biome
#        with:
#          path: ~/.hab/cache
#          key: ${{ runner.os }}-build-${{ env.CACHE_NAME }}-${{ hashFiles('go17/plan.sh') }}
#          restore-keys: |
#            ${{ runner.os }}-build-${{ env.CACHE_NAME }}-
#            ${{ runner.os }}-build-
#            ${{ runner.os }}-
#
#      - name: Test
#        env:
#          HAB_ORIGIN: "themelio"
#          BIOME_PUBLIC_KEY: ${{ secrets.BIOME_PUBLIC_KEY }}
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#        run: bio pkg build go17
#
#
##      - name: Build, Run BATS Tests
##        env:
##          HAB_ORIGIN: "themelio"
##          BIOME_PUBLIC_KEY: ${{ secrets.BIOME_PUBLIC_KEY }}
##          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
##        run: ./biome/themelio-node/scripts/ci-test.sh





#jobs:
#  check:
#    name: Check files
#    outputs:
#      run_job: ${{ steps.check_files.outputs.run_job }}
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v2
#        with:
#          fetch-depth: 2
#
#      - name: check modified files
#        id: check_files
#        run: |
#          echo "=============== list modified files ==============="
#          git diff --name-only HEAD^ HEAD
#
#          echo "========== check paths of modified files =========="
#          git diff --name-only HEAD^ HEAD > files.txt
#          while IFS= read -r file
#          do
#            echo $file
#            if [[ $file != db/* ]]; then
#              echo "This modified file is not under the 'go17' folder."
#              echo "::set-output name=run_job::false"
#              break
#            else
#              echo "::set-output name=run_job::true"
#            fi
#          done < files.txt
#
#  job_for_db:
#    name: Job for 'go17' folder
#    needs: check
#    if: needs.check.outputs.run_job == 'true'
#    runs-on: ubuntu-latest
#    steps:
#      - name: run for 'go17' folder
#        run: echo "This job runs only when all the modified files are under the 'go17' folder."